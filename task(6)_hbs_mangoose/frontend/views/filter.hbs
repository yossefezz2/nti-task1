{{>nav}}
<form action="/filterActive" method="post">
    <div class="form-floating m-2 mb-3 fs-6">
        <input type="text" class="form-control" name="filter" placeholder="enter filter" {{#if filter}} value="{{filter}}" {{/if}} required>
        <label for="floatingInput">filter</label>
    <div class="d-flex justify-content-center mt-5">
        <button  class="btn btn-success mx-3 fs-5 mt-2">edit User</button>
    </div>
</form>

{{#if task}}

{{>listfilter}} 
{{/if}}

{{>footer}}